[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "ws-competitions"
description = "Test for a competitive format change for 4 person curling"
authors = [{name = "Cardoso", email = "joao.cardoso@fdiportugal.pt"}]
dynamic = ["version"]
